# When you return

- **Run the lab:** `python3 -m http.server 8765` → http://localhost:8765  
- **OSC to the room:** start the relay in `../../studio/light-art-osc/bridge/` first (`node ws_to_osc.cjs`), then enable **OSC export** in the UI. Full steps: `LIGHT_ART_023_OSC.md`.

Broader stack reminder: `../../studio/light-art-osc/WHEN_YOU_RETURN.md`
